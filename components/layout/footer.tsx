const Footer = () => {
   return (
      <footer className="bg-dark text-center text-white p-5">
         This is a footer and it's awesome | {new Date().getFullYear()}
      </footer>
   );
};

export default Footer;
